# agent.md

## 项目目标

本项目的整体目标见 [计划.md](D:\龙东苗\ai-bid\计划.md)。

一句话概括：
给定一个招标文件，逐步产出可编辑、可继续自动填充、最终可形成投标文件的中间结果与成品。

当前希望形成的主流程是：
1. 读取招标文件。
2. 提取“投标/响应文件格式”部分，生成投标模板。
3. 从招标文件中提取项目名称、编号、偏离表等结构化信息。
4. 结合公司通用资料，按评审办法和评分标准构建投标文件内容框架。
5. 对项目专属信息进行人工澄清后再回填。
6. 在最终交付前做标书检查，并识别异常、特殊或高风险要求。

## 当前实际能力

结合仓库当前内容，已经落地的能力主要有 3 类：

### 1. 文档技能
- 目录：`skills/文档`
- 用途：读取、创建、编辑、渲染 `.docx` 文档，适合做版式检查和文档生成。
- 适用场景：查看招标文件内容、生成中间文档、检查导出的 docx 是否可用。

### 2. 投标模板提取
- 目录：`skills/投标模板提取`
- 用途：从单个 `.docx/.doc/.pdf` 招标文件中，定位“响应文件格式/投标文件格式”等章节，并做定向抽取。
- 特点：边界由 agent 做语义判断，脚本负责确定性提取。
- 当前这是最成熟、最贴近主流程第 1 步和第 2 步的能力。

### 3. 商务和技术偏离表
- 目录：`skills/商务和技术偏离表`
- 用途：针对“商务和技术偏离表”这类表格，先做语义拆行，再将结果回填进模板。
- 特点：保留通用扫描、校验、回填能力；每个项目的抽取逻辑仍允许临时脚本定制。
- 当前这是主流程第 3 步里最明确、最可执行的一块。

## 当前未完整实现的部分

以下目标在 [计划.md](D:\龙东苗\ai-bid\计划.md) 中已明确，但仓库当前还没有完整落地：

- 基于“评审办法 / 评分标准”自动构建整份投标文件框架。
- 自动把公司通用资料映射到各章节并完成大规模内容填充。
- 项目专属信息的澄清对话与回填闭环。
- 面向整份标书的系统化检查流程。
- 更开放的“创新方案/头脑风暴”能力沉淀。

因此，现阶段不要把这个项目描述成“已完成端到端自动写标书”。
更准确的说法是：
当前已经完成了“文档读取 + 模板提取 + 偏离表专项处理”的基础能力搭建。

## 推荐工作方式

当 agent 在这个仓库中工作时，优先遵循下面的顺序：

1. 如果任务是读取、检查、渲染、生成 docx，优先使用 `skills/文档`。
2. 如果任务是从招标文件中提取“投标文件格式/响应文件格式”，优先使用 `skills/投标模板提取`。
3. 如果任务是处理“商务和技术偏离表”，优先使用 `skills/商务和技术偏离表`。
4. 如果任务超出当前已实现能力，应先明确这是“新增能力开发”，不要伪装成已有功能。

## 当前主线范围

如果没有额外说明，当前主线优先级应当是：

1. 打磨“投标模板提取”流程，让它稳定处理常见 `.docx/.doc/.pdf` 招标文件。
2. 打磨“商务和技术偏离表”流程，让它能稳定产出 `rows.json` 并回填模板。
3. 沿着 [计划.md](D:\龙东苗\ai-bid\计划.md) 的目标，逐步补齐结构化信息提取、资料映射、标书检查等能力。

## 边界与约束

- “章节起止边界”必须以语义判断为主，不能只靠关键词硬匹配。
- 可复用脚本应保持通用，不要把某一个项目的特殊规则直接写死进长期 skill。
- 某次运行特有的判断，优先放进 `tmp/docs/<run-name>/` 下的临时脚本或决策文件。
- 输出文档默认应保持可编辑，除非用户明确接受图片化结果。
- 若招标文件结构含糊、存在多种合理解释，应暂停并提示人工确认，而不是强行自动决策。

## Skill 约束

- 后续工作中，如工程内已有可用 skill，默认优先使用本工程 `skills/` 目录下的 skill，而不是依赖工程外的同类能力。
- 如果现有 skill 不足以完成任务，应优先在本工程内继续打磨、扩展或新增 skill，使能力沉淀在当前仓库。
- 新创建的 skill 默认放在 `skills/` 目录下，不要散落到其他位置。
- 新创建的 skill 默认使用中文命名，目录名、`SKILL.md` 中的 `name`、对外引用名称应尽量保持一致。
- 只有在工程内 skill 明显不适合、且短期内没有沉淀必要时，才考虑临时使用工程外能力；此时应明确说明原因。

## 成功标准

在当前阶段，一个“成功”的任务通常意味着：

- 能从招标文件中稳定提取目标模板章节。
- 产出的模板是可编辑的 `.docx`，且结构基本可用。
- 对偏离表任务，能得到可校验的 `rows.json` 和可回填的输出文档。
- 每一步都能留下清晰的中间产物，便于人工复核与后续自动化接力。
